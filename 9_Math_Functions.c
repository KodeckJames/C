#include <stdio.h>
#include <math.h>

int main(){

    //Square Root Function
    double A = sqrt(9);

    //Raising a number to a power
    double B = pow(2, 4);

    //Rounding off a number
    int C = round(3.14);

    //Rounding a number up using ceil
    int D = ceil(3.14);

    //Rounding down number up using floor
    int E = floor(3.14);

    //Absolute value of a number using fabs ie how far away it is from 0
    double F = fabs(-389);

    //Logarithm of a number
    double G = log(3);

    //sin
    double H = sin(45);

    //cos
    double I = cos(45);
    
    //tan
    double J = tan(45);

    printf("%lf", J);
}