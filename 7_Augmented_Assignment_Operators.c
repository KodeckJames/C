#include<stdio.h>

int main(){
    //Augmented assignment  operators => Used to place a statement where an operator takes a variable as one of its arguments then assigns the result back to the same variable ie x = x +1 :: x+=1, x-=2, x*=3, x/=4, x%=3
    
}