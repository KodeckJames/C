#include <stdio.h>

int main(){
    //Constants are defined by preceding the variable with const, then it's advisable to capitalize the variable name

    const float PI = 3.14159;

    printf(" PI is %f ", PI);

    return 0;
}